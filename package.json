{
  "dependencies": {
    "@asyncapi/cli": "^3.5.2"
  },
  "scripts": {
    "generate:models": "asyncapi generate models java ./asyncapi.yaml --packageName=com.example.models -o ./vah-regel-rtf-spec --javaJackson"
  }
}
